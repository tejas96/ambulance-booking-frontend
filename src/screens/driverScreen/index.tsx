import React from 'react';
import {Text} from 'react-native-paper';
import {SafeAreaView} from '../../components';

interface IProps {}
const DriverLandingScreen: React.FC<IProps> = () => {
  return (
    <SafeAreaView>
      <Text>DriverLandingScreen</Text>
    </SafeAreaView>
  );
};

export default DriverLandingScreen;
