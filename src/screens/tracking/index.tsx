import React from 'react';
import {Typography, SafeAreaView} from '../../components/';

const Tracking: React.FC<{}> = () => {
  return (
    <SafeAreaView>
      <Typography type="title">Tracking</Typography>
    </SafeAreaView>
  );
};

export default Tracking;
